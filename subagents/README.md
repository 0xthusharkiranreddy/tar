# Subagent Helpers

These are optional helper wrappers for isolating narrow background branches.

## Included

- `recon-agent.sh`
  Optional scaffold for background recon tasks.

- `vuln-agent.sh`
  Optional scaffold for isolated payload-rotation or vulnerability branch work.

## Status

These scripts are optional automation helpers. They are not canonical runtime requirements and are not part of the preservation layer.

The authoritative subagent guidance lives in:

- `/home/kali/knowledge/workflows/subagent-design.md`
